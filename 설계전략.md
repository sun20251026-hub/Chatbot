# Q\&A 챗봇 — 설계 전략

## 1\. 전체 구조

```
사용자 인터넷 프로그램    ↕
Streamlit 서버 (Python)
    ├── FAISS 벡터 검색 (PDF 지식베이스)
    └── Anthropic Claude API (claude-sonnet-4-6)
```

백엔드와 UI를 Streamlit 단일 프로세스로 운영해 API 키가 브라우저에 노출되지 않는 구조를 유지한다.

\---

## 2\. 지식 계층 분리

|계층|내용|저장 위치|
|-|-|-|
|고정 지식|일반 상식|코드 내 시스템 프롬프트|
|동적 지식|PDF 업로드 자료 (딥러닝 등 외부 문서)|FAISS 인덱스|

고정 지식은 매 요청마다 시스템 프롬프트에 포함되고, 동적 지식은 질문과의 유사도 검색 후 컨텍스트에 추가된다.

\---

## 3\. RAG 파이프라인

### 3-1. 인덱싱 (오프라인)

```
PDF 파일 (pdf/)
  → PyMuPDF로 텍스트 추출
  → 문장 단위 분리 후 400자 청크 / 80자 오버랩
  → multilingual-e5-small 임베딩 (passage: 접두사)
  → FAISS IndexFlatIP 저장
      faiss.index  — 벡터
      chunks.pkl   — 텍스트 + 메타데이터
```

### 3-2. 검색 (런타임)

```
사용자 질문
  → "query: " 접두사 + 임베딩
  → FAISS 내적 유사도 검색 (top-k)
  → 청크 텍스트 + 출처 조합
  → Claude 시스템 프롬프트에 추가
```

\---

## 4\. 핵심 기술 선택 근거

### 벡터 DB — FAISS

* **선택 이유**: ChromaDB 대비 패키지 경량, SQLite 의존성 없음
* `faiss.index` + `chunks.pkl` 파일 2개로 저장 → git 관리 가능
* `IndexFlatIP` (내적) + 정규화 벡터 = 코사인 유사도와 동일

### 임베딩 모델 — intfloat/multilingual-e5-small

* **선택 이유**: multilingual-MiniLM-L12-v2(470MB) 대비 \~120MB로 경량
* 한국어·영어 혼합 문서에 충분한 품질
* e5 계열은 `query:` / `passage:` 접두사로 검색·문서 임베딩을 분리해 정밀도 향상

### LLM — Claude claude-sonnet-4-6

* 한국어 생성 품질 및 긴 컨텍스트 처리에 적합

\---

## 5\. 보안 설계

### 시크릿 관리 우선순위

```
st.secrets (Streamlit Cloud)
  → os.environ / .env (로컬 개발)
  → UI 직접 입력 (폴백)
```

`python-dotenv`로 `.env`를 로컬에서 로드하고, 배포 시에는 Streamlit Secrets를 사용한다. `.env`는 `.gitignore`에 포함해 레포에 올리지 않는다.

### 관리자 잠금

* 인덱스 빌드 기능은 비밀번호 인증 후에만 노출
* 비밀번호는 `ADMIN\_PASSWORD` 환경변수로 관리
* `st.session\_state`로 세션 단위 잠금 유지

### API 키 노출 방지

* Streamlit은 Python 서버 사이드에서 API를 호출하므로 키가 브라우저에 노출되지 않음
* Lovable·Vercel 등 프론트엔드 빌드 환경으로 전환 시 키 노출 위험 발생

\---

## 6\. 파일 구조

```
streamsample/
├── main.py          Streamlit Q\&A 앱 (RAG 연동)
├── rag.py           RAG 파이프라인 모듈 (인덱싱·검색)
├── visualize.py     청크 시각화 앱 (개요·탐색·PCA)
├── requirements.txt
├── .env.example     환경변수 예시 (git 포함)
├── .env             실제 키 (git 제외)
├── pdf/             원본 PDF 보관
├── faiss.index      FAISS 벡터 인덱스 (git 포함 가능)
└── chunks.pkl       청크 텍스트·메타데이터 (git 포함 가능)
```

\---

## 7\. 배포 전략

|환경|방법|
|-|-|
|로컬 개발|`.env` + `streamlit run main.py`|
|Streamlit Cloud|private 레포 연결 + Secrets 설정|
|인덱스 관리|`faiss.index` / `chunks.pkl`을 git에 포함하거나, 배포 후 관리자 로그인 → 빌드 버튼으로 생성|

Streamlit Cloud는 앱 재시작 시 파일시스템이 초기화되므로, PDF가 많다면 인덱스 파일을 git에 포함하는 방식이 안정적이다.

\---

## 8\. 확장 포인트

* **임베딩 모델 교체**: `rag.py`의 `EMBED\_MODEL` 상수만 변경
* **청크 크기 조정**: `build\_index(chunk\_size=, overlap=)` 파라미터로 제어
* **다국어 지원**: e5-small 모델이 100개 언어 지원하므로 별도 처리 불필요
* **검색 방식 변경**: `IndexFlatIP` → `IndexIVFFlat` 교체로 대규모 청크(수십만) 대응 가능

